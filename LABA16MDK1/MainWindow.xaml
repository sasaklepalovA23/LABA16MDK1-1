<Window x:Class="LABA16MDK1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LABA16MDK1"
        mc:Ignorable="d"
                     Title="Лабораторная работа: Анимации и Эффекты" Height="600" Width="950"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="5" BorderBrush="White" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" 
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="#34495E" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="FontSize" To="15" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" 
                                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                            To="Transparent" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="FontSize" To="14" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#FFDA4EAE">
            <StackPanel.Effect>
                <DropShadowEffect BlurRadius="15" Direction="0" ShadowDepth="2" Opacity="0.5"/>
            </StackPanel.Effect>

            <TextBlock Text="ЛАБА 16" Foreground="#ECF0F1" FontSize="24" FontWeight="Bold" 
                       Margin="20" HorizontalAlignment="Center" Background="#FFDF82C8">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#FFE31AD7" BlurRadius="5" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>

            <Separator Background="#FFEB2ACA" Margin="20,10"/>
            <Button Content="Задание 1" Style="{StaticResource MenuButtonStyle}" Click="GoTask1"/>
            <Button Content="Задание 2" Style="{StaticResource MenuButtonStyle}" Click="GoTask2"/>
            <Button Content="Задание 3" Style="{StaticResource MenuButtonStyle}" Click="GoTask3"/>
            <Button Content="Задание 4" Style="{StaticResource MenuButtonStyle}" Click="GoTask4"/>
            <Button Content="Задание 5" Style="{StaticResource MenuButtonStyle}" Click="GoTask5"/>
        </StackPanel>

        <Grid Grid.Column="1" Background="#F5F6FA">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden">
                <Frame.Triggers>

                    <EventTrigger RoutedEvent="Frame.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.8"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Frame.Triggers>
            </Frame>
        </Grid>
    </Grid>
</Window>
