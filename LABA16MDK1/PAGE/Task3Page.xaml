<Page x:Class="LABA16MDK1.PAGE.Task3Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LABA16MDK1.PAGE"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
            Title="Задание 3: Анимация">
    <Page.Style
>
        <Style TargetType="Page">
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:1.5"/>
                            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,50,0,0" To="0" Duration="0:0:1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Page.Style>
    <Grid Background="#F9F9F9">
        <Border Background="White" CornerRadius="20" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="LightGray" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Задание 3: Плавное появление" FontSize="24" HorizontalAlignment="Center"/>
                <Rectangle Width="200" Height="100" Fill="Purple" Margin="20">
                    <Rectangle.RenderTransform>
                        <RotateTransform Angle="10"/>
                    </Rectangle.RenderTransform>
                </Rectangle>
                <TextBlock Text="Эта страница появилась с анимацией загрузки" Foreground="Gray"/>
            </StackPanel>
        </Border>
    </Grid>
</Page> 