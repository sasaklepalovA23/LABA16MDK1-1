<Page x:Class="LABA16MDK1.PAGE.Task4Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LABA16MDK1.PAGE"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
          Title="Задание 4: DataTrigger">
    <Grid Background="White">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <CheckBox x:Name="StatusCheck" Content="Пометить как выполненное" FontSize="16" Margin="10"/>
            <TextBlock Text="Текст задания для проверки" FontSize="20" Margin="10">
                <TextBlock.Style
>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="Black"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=StatusCheck}" Value="True">
                                <Setter Property="Foreground" Value="Green"/>
                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                <Setter Property="FontStyle" Value="Italic"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <Border BorderBrush="Gray" BorderThickness="1" Height="2" Margin="10"/>
            <TextBlock Text="Используется DataTrigger для изменения свойств в зависимости от CheckBox" Foreground="LightGray"/>
        </StackPanel>
    </Grid>
</Page>