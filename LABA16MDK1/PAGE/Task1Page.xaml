<Page x:Class="LABA16MDK1.PAGE.Task1Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LABA16MDK1.PAGE"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
            Title="Task1Page">
    <Page.Resources>
        <Style TargetType="Button" x:Key="AnimatedButtonStyle">
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="SkyBlue"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>

                <EventTrigger RoutedEvent="Button.PreviewMouseDown">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="0.9" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="0.9" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>

                <EventTrigger RoutedEvent="Button.PreviewMouseUp">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1.0" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox" x:Key="ValidationTextBox">
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
            <Style.Triggers>

                <Trigger Property="Text" Value="">
                    <Setter Property="BorderBrush" Value="Gray"/>
                </Trigger>

                <Trigger Property="Tag" Value="Valid">
                    <Setter Property="Background" Value="#E8F5E9"/>

                    <Setter Property="BorderBrush" Value="Green"/>
                </Trigger>
                <Trigger Property="Tag" Value="Invalid">
                    <Setter Property="Background" Value="#FFEBEE"/>

                    <Setter Property="BorderBrush" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="White">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">

            <TextBlock Text="Задание 1: Триггеры" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="Введите число от 1 до 100:"/>

            <TextBox x:Name="inputField" 
                     Style="{StaticResource ValidationTextBox}" 
                     TextChanged="inputField_TextChanged"/>

            <Button Content="Нажми на меня" 
                    Style="{StaticResource AnimatedButtonStyle}" 
                    Margin="0,20,0,0"/>

        </StackPanel>
    </Grid>
</Page>